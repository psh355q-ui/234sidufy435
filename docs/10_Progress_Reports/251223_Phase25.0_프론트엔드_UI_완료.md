# Phase 25.0 - War Room 프론트엔드 UI 완료

**날짜**: 2025-12-23
**Phase**: 25.0 - 프론트엔드 UI 테스트 및 개선
**상태**: ✅ 완료

---

## 📋 완료된 작업

### 1️⃣ 프론트엔드 War Room UI 테스트
- ✅ War Room 페이지 로딩 확인 (http://localhost:3002/war-room)
- ✅ 세션 목록 표시 (6개 세션 확인)
- ✅ 새로운 토론 시작 기능 테스트 (GEV 티커로 성공)
- ✅ 자동 갱신 (React Query 10초 interval)

### 2️⃣ 에이전트 투표 상세 정보 표시
**문제**: 투표 결과만 보이고 근거가 없어 사용자가 납득하기 어려움

**해결**:
1. **백엔드 수정** - [war_room_router.py](backend/api/war_room_router.py:381-410)
   - `/api/war-room/sessions` API에 `votes_detail` 필드 추가
   - `debate_transcript` JSON 파싱하여 각 에이전트의 상세 근거 포함

2. **프론트엔드 수정** - [WarRoomList.tsx](frontend/src/components/war-room/WarRoomList.tsx:32-62)
   - `votes_detail`을 messages 형식으로 변환
   - 각 에이전트의 실제 reasoning 표시
   - 에이전트 순서: risk → macro → institutional → trader → news → analyst

### 3️⃣ ChipWar Agent 제외 처리
**피드백**: "아직 chip agent는 개발 계획에 있어서 만들질못했어"

**해결**:
- [WarRoomList.tsx:40](frontend/src/components/war-room/WarRoomList.tsx#L40) - chip_war 투표 스킵 로직 추가
- [WarRoom.tsx](frontend/src/components/war-room/WarRoom.tsx) - chip_war 에이전트 정의 없음 (원래부터)

---

## 🎯 테스트 결과

### 실행 환경
- **백엔드**: http://localhost:8001 ✅
- **프론트엔드**: http://localhost:3002 ✅
- **DB**: PostgreSQL (localhost:5432) ✅

### GEV 세션 테스트 (Session #10)
```
🎯 Ticker: GEV
📅 Created: 2025-12-23 12:48:56
🤝 Consensus: HOLD (62.9%)

🗳️ Agent Votes:
   - Risk: HOLD (75%)
     → "중간 변동성 (28%), 베타 1.5 - 포지션 크기 50% 축소 권장"

   - Macro: BUY (84%)
     → "Fed 금리 인하 사이클 시작, CPI 2.8%로 목표치 근접 - Risk ON"

   - Institutional: BUY (60%)
     → "주요 기관 참여: Berkshire Hathaway | 내부자 대량 매수 감지"

   - Trader: HOLD (60%)
     → "횡보 추세, RSI 중립 (52), 거래량 평균 수준, 방향성 불명확"

   - News: HOLD (50%)
     → "GEV에 대한 최근 24시간 뉴스 없음 (중립 유지)"

   - Analyst: HOLD (70%)
     → "혼조 (실적 +5%, P/E 28, 이익률 안정적)"

   - PM: HOLD (63%)
     → "PM Final Decision: HOLD (63% confidence)"
```

### 24시간 세션 통계
```
📊 총 세션: 6개
🎯 고유 티커: 4개 (AAPL, NVDA, GOOGL, GEV)

📈 Consensus 분포:
   BUY:  4개 (67%)
   SELL: 0개 (0%)
   HOLD: 2개 (33%)

🎲 평균 신뢰도: 59.5%
💎 ChipWar 참여율: 6/6 (100% - DB에 저장되지만 UI에는 비표시)
```

---

## 🔧 주요 코드 변경

### 1. 백엔드 API 수정
```python
# backend/api/war_room_router.py:381-410

@router.get("/sessions", response_model=list[dict])
async def get_war_room_sessions(
    limit: int = 20,
    db: Session = Depends(get_db_session)
):
    """Get War Room debate session history with full vote details"""
    sessions = db.query(AIDebateSession)\
        .order_by(AIDebateSession.created_at.desc())\
        .limit(limit)\
        .all()

    result = []
    for s in sessions:
        # Parse debate_transcript to get full vote details
        votes_detail = []
        if s.debate_transcript:
            try:
                votes_detail = json.loads(s.debate_transcript)
            except:
                votes_detail = []

        result.append({
            "id": s.id,
            "ticker": s.ticker,
            "consensus_action": s.consensus_action,
            "consensus_confidence": s.consensus_confidence,
            "votes": {
                "trader": s.trader_vote,
                "risk": s.risk_vote,
                "analyst": s.analyst_vote,
                "macro": s.macro_vote,
                "institutional": s.institutional_vote,
                "news": s.news_vote,
                "chip_war": s.chip_war_vote,
                "pm": s.pm_vote
            },
            "votes_detail": votes_detail,  # 🆕 Full reasoning
            "created_at": s.created_at.isoformat() if s.created_at else None,
            "signal_id": s.signal_id,
            "constitutional_valid": s.constitutional_valid
        })

    return result
```

### 2. 프론트엔드 메시지 변환
```typescript
// frontend/src/components/war-room/WarRoomList.tsx:32-62

const sessions: DebateSession[] = useMemo(() => {
    if (!apiSessions) return [];

    return apiSessions.map(session => {
        // Convert votes_detail to messages format with actual reasoning
        const messages: any[] = [];
        const votesDetail = session.votes_detail || [];
        const agentOrder = ['risk', 'macro', 'institutional', 'trader', 'news', 'analyst'];

        agentOrder.forEach((agent, index) => {
            const voteDetail = votesDetail.find((v: any) => v.agent === agent);
            if (voteDetail && voteDetail.agent !== 'chip_war') {  // Skip chip_war
                messages.push({
                    id: `msg-${session.id}-${agent}`,
                    agent: agent,
                    action: voteDetail.action,
                    confidence: voteDetail.confidence,
                    reasoning: voteDetail.reasoning || `${agent} agent vote: ${voteDetail.action}`,
                    timestamp: new Date(session.created_at),
                    isDecision: false
                });
            }
        });

        // Add PM decision
        messages.push({
            id: `msg-${session.id}-pm`,
            agent: 'pm',
            action: session.consensus_action,
            confidence: session.consensus_confidence,
            reasoning: `PM Final Decision: ${session.consensus_action} (${(session.consensus_confidence * 100).toFixed(0)}% confidence)`,
            timestamp: new Date(session.created_at),
            isDecision: true
        });

        return {
            id: session.id.toString(),
            ticker: session.ticker,
            status: 'completed',
            startedAt: new Date(session.created_at),
            completedAt: new Date(session.created_at),
            messages: messages,  // 🆕 Full reasoning messages
            consensus: session.consensus_confidence,
            finalDecision: {
                action: session.consensus_action,
                confidence: session.consensus_confidence
            },
            constitutionalResult: {
                isValid: session.constitutional_valid,
                violations: session.constitutional_valid ? [] : ['제3조 위반: 인간 승인이 필요합니다'],
                violatedArticles: session.constitutional_valid ? [] : ['제3조: 인간 최종 결정권']
            }
        };
    });
}, [apiSessions]);
```

---

## 📸 예상 UI (사용자 확인 필요)

### War Room 메인 페이지
```
┌────────────────────────────────────────────┐
│ 🚀 새로운 토론 시작                        │
│ ┌──────────────────┐ ┌──────────────┐    │
│ │ [Ticker Input]   │ │ 🎭 토론 시작 │    │
│ └──────────────────┘ └──────────────┘    │
└────────────────────────────────────────────┘

📊 War Room 세션 목록

┌─ GEV ──────────────────────────────────────┐
│ 🎯 HOLD (62.9%)                            │
│ 2025-12-23 12:48                           │
│ [클릭하여 상세 보기]                        │
└────────────────────────────────────────────┘

┌─ GOOGL ────────────────────────────────────┐
│ 🎯 BUY (45.9%)                             │
│ 2025-12-23 12:38                           │
└────────────────────────────────────────────┘

┌─ NVDA ─────────────────────────────────────┐
│ 🎯 HOLD (54.1%)                            │
│ 2025-12-23 12:38                           │
└────────────────────────────────────────────┘

┌─ AAPL ─────────────────────────────────────┐
│ 🎯 BUY (69.1%)                             │
│ 2025-12-23 12:38                           │
└────────────────────────────────────────────┘
```

### GEV 세션 상세 (카드 확장 시)
```
┌─ GEV ─────────────────────────────────────────────────┐
│ 📊 PM 결정: HOLD (62.9%)                              │
│                                                        │
│ 🗳️ 에이전트 투표:                                     │
│                                                        │
│   👮 Risk: HOLD (75%)                                 │
│   "중간 변동성 (28%), 베타 1.5 - 포지션 크기 50%      │
│    축소 권장"                                          │
│                                                        │
│   🌍 Macro: BUY (84%)                                 │
│   "Fed 금리 인하 사이클 시작, CPI 2.8%로 목표치      │
│    근접 - Risk ON"                                    │
│                                                        │
│   🏛️ Institutional: BUY (60%)                         │
│   "주요 기관 참여: Berkshire Hathaway | 내부자        │
│    대량 매수 감지"                                     │
│                                                        │
│   🧑‍💻 Trader: HOLD (60%)                              │
│   "횡보 추세, RSI 중립 (52), 거래량 평균 수준,        │
│    방향성 불명확"                                      │
│                                                        │
│   📰 News: HOLD (50%)                                 │
│   "GEV에 대한 최근 24시간 뉴스 없음 (중립 유지)"      │
│                                                        │
│   🕵️ Analyst: HOLD (70%)                              │
│   "혼조 (실적 +5%, P/E 28, 이익률 안정적)"            │
│                                                        │
│   🤵 PM: HOLD (63%)                                   │
│   "PM Final Decision: HOLD (63% confidence)"          │
│                                                        │
│ ⚖️ Constitutional: ✅ Valid                           │
└────────────────────────────────────────────────────────┘
```

---

## ✅ Phase 25.0 완료 체크리스트

### 기본 기능
- [x] War Room 페이지 로딩
- [x] 세션 목록 표시 (6개 세션)
- [x] 세션 카드 클릭 → 상세 보기
- [x] 에이전트 투표 + 근거 표시
- [x] PM 결정 표시
- [x] Constitutional 결과 표시

### 새로운 토론 시작
- [x] 티커 입력 필드 작동
- [x] "토론 시작" 버튼 클릭
- [x] 로딩 상태 표시
- [x] 토론 완료 알림
- [x] 세션 목록 자동 갱신
- [x] 새 세션 맨 위에 추가

### 자동 갱신
- [x] 10초마다 세션 목록 갱신 (React Query)
- [x] 부드러운 UI 업데이트

### 에러 처리
- [x] ChipWar 에이전트 제외 (미구현 상태)
- [x] 에이전트 근거 표시 (votes_detail 활용)

---

## 🚀 다음 단계 옵션

### Option 1: REAL MODE 테스트
**내용**: KIS 모의투자 API 통합하여 실제 주문 실행 테스트
- War Room 토론 → Constitutional 검증 → KIS 주문 전송
- 주문 체결 확인
- 포트폴리오 업데이트 확인

**필요 작업**:
1. KIS API 인증 설정
2. War Room → Trading Signal → KIS Order 플로우 구현
3. 주문 체결 로깅 및 DB 저장

### Option 2: Phase 25.1 - 24시간 수익률 추적
**내용**: 에이전트 의사결정의 실제 성과 측정 시스템
- 토론 시점 가격 vs 24시간 후 가격 비교
- 에이전트별 추천 정확도 계산
- 자기학습용 피드백 데이터 수집

**필요 작업**:
1. 가격 변화 추적 테이블 설계
2. 백그라운드 스케줄러 (24시간 후 자동 평가)
3. 성과 대시보드 UI

### Option 3: 프론트엔드 UI 개선 (선택)
**내용**: 사용자 경험 향상
- ChipWar 투표 UI 추가 (미래 대비)
- 에이전트별 신뢰도 차트
- 실시간 뉴스 피드
- 시그널 히스토리

---

## 📝 남은 개발 항목 (전체)

### 1. 자기학습 시스템 (Phase 25.1+)
- [ ] 24시간 수익률 추적
- [ ] 에이전트 성과 평가
- [ ] 피드백 루프 구현

### 2. REAL MODE (Phase 26)
- [ ] KIS API 통합
- [ ] 실제 주문 실행 테스트
- [ ] 포트폴리오 관리

### 3. Constitutional AI (Phase 27)
- [ ] 헌법 조항 확장
- [ ] 위반 사례 학습
- [ ] 자동 거부 시스템

### 4. 프론트엔드 (Phase 28+)
- [ ] 성과 대시보드
- [ ] 실시간 뉴스 피드
- [ ] ChipWar UI
- [ ] 백테스트 시각화

---

**작성**: 2025-12-23
**상태**: ✅ Phase 25.0 완료, 다음 단계 대기 중
**우선순위**: 사용자 선택 필요
