# Phase 25.3 - 에이전트별 성과 추적 시스템 완료

**날짜**: 2025-12-23
**Phase**: 25.3 - Agent-Specific Performance Tracking & Self-Learning Feedback
**상태**: ✅ 완료

---

## 📋 완료된 작업

### 1️⃣ agent_vote_tracking 테이블 생성

**파일**: [backend/database/create_agent_vote_tracking_table.sql](../../backend/database/create_agent_vote_tracking_table.sql)

**테이블 구조**:
```sql
CREATE TABLE agent_vote_tracking (
    id SERIAL PRIMARY KEY,
    session_id INTEGER REFERENCES ai_debate_sessions(id),
    price_tracking_id INTEGER REFERENCES price_tracking(id),

    -- Agent info
    agent_name VARCHAR(50) NOT NULL,  -- trader, analyst, risk, macro, institutional, news

    -- Vote details
    vote_action VARCHAR(10) NOT NULL,  -- BUY, SELL, HOLD
    vote_confidence NUMERIC(5, 4) NOT NULL,
    vote_reasoning TEXT,

    -- Ticker info
    ticker VARCHAR(20) NOT NULL,
    initial_price NUMERIC(10, 2) NOT NULL,
    initial_timestamp TIMESTAMP NOT NULL,

    -- 24h evaluation
    final_price NUMERIC(10, 2),
    final_timestamp TIMESTAMP,
    return_pct NUMERIC(10, 4),

    -- Performance metrics
    is_correct BOOLEAN,
    performance_score NUMERIC(5, 4),

    -- Status
    status VARCHAR(20) DEFAULT 'PENDING',
    created_at TIMESTAMP DEFAULT NOW(),
    evaluated_at TIMESTAMP
);
```

**인덱스**:
- `idx_agent_vote_tracking_agent_name` - 에이전트별 조회
- `idx_agent_vote_tracking_status` - 상태별 조회
- `idx_agent_vote_tracking_created_at` - 시간순 조회
- `idx_agent_vote_tracking_session_id` - 세션별 조회
- `idx_agent_vote_tracking_ticker` - 티커별 조회
- `idx_agent_vote_tracking_agent_status` - 복합 인덱스

---

### 2️⃣ 개별 에이전트 투표 저장 로직

**파일**: [backend/api/war_room_router.py](../../backend/api/war_room_router.py)

**새 함수**: `save_agent_votes_tracking()`

**기능**:
- War Room 토론 완료 시 각 에이전트의 투표를 `agent_vote_tracking` 테이블에 저장
- PM 에이전트는 제외 (consensus는 `price_tracking`에서 추적)
- 각 에이전트의 action, confidence, reasoning 저장

**코드** ([war_room_router.py:346-395](../../backend/api/war_room_router.py#L346-L395)):
```python
async def save_agent_votes_tracking(
    session_id: int,
    ticker: str,
    debate_transcript: List[Dict[str, Any]],
    current_price: float,
    db: Any
) -> None:
    """Save individual agent votes for 24-hour tracking"""

    for vote in debate_transcript:
        agent_name = vote.get("agent")
        vote_action = vote.get("action")
        vote_confidence = vote.get("confidence", 0.5)
        vote_reasoning = vote.get("reasoning", "")

        # Skip PM agent
        if agent_name == "pm":
            continue

        # INSERT into agent_vote_tracking
        ...
```

**통합** ([war_room_router.py:331-338](../../backend/api/war_room_router.py#L331-L338)):
- `save_initial_price_tracking` 함수에서 `save_agent_votes_tracking` 호출
- War Room 토론 후 consensus + 개별 투표 동시 저장

---

### 3️⃣ 에이전트별 24시간 후 평가 로직

**파일**: [backend/automation/price_tracking_scheduler.py](../../backend/automation/price_tracking_scheduler.py)

**새 함수**: `evaluate_agent_votes_tracking()`

**기능**:
1. PENDING 상태이고 24시간 이상 경과한 에이전트 투표 조회
2. KIS API로 현재 가격 조회
3. 수익률 계산
4. 성과 평가 (BUY/SELL/HOLD 정답 여부)
5. DB 업데이트 (PENDING → COMPLETED)

**평가 로직**:
```python
# evaluate_performance() 함수 재사용
# BUY: return_pct > 0 → correct
# SELL: return_pct < 0 → correct
# HOLD: abs(return_pct) < 2% → correct
# performance_score = return_pct * confidence (weighted)
```

**실행**:
```bash
python backend/automation/price_tracking_scheduler.py
# 자동으로 consensus + agent votes 모두 평가
```

---

### 4️⃣ 에이전트별 성과 API 엔드포인트

**파일**: [backend/api/performance_router.py](../../backend/api/performance_router.py)

#### GET /api/performance/by-agent
에이전트별 전체 성과

**Response**:
```json
[
  {
    "agent_name": "trader",
    "total_votes": 50,
    "correct_votes": 36,
    "accuracy": 72.0,
    "avg_return": 1.45,
    "avg_performance_score": 0.0102
  },
  {
    "agent_name": "analyst",
    "total_votes": 50,
    "correct_votes": 34,
    "accuracy": 68.0,
    "avg_return": 1.12,
    "avg_performance_score": 0.0089
  },
  {
    "agent_name": "risk",
    "total_votes": 50,
    "correct_votes": 30,
    "accuracy": 60.0,
    "avg_return": 0.85,
    "avg_performance_score": 0.0065
  }
]
```

**SQL 쿼리**:
```sql
SELECT
    agent_name,
    COUNT(*) as total_votes,
    SUM(CASE WHEN is_correct THEN 1 ELSE 0 END) as correct_votes,
    ROUND(
        CAST(SUM(CASE WHEN is_correct THEN 1 ELSE 0 END) AS NUMERIC) /
        NULLIF(COUNT(*), 0) * 100, 2
    ) as accuracy,
    ROUND(AVG(return_pct), 4) as avg_return,
    ROUND(AVG(performance_score), 4) as avg_performance_score
FROM agent_vote_tracking
WHERE status = 'COMPLETED'
GROUP BY agent_name
ORDER BY accuracy DESC
```

#### GET /api/performance/by-agent-action?agent=trader
에이전트별 액션별 성과

**Query Parameters**:
- `agent` (optional): 특정 에이전트 필터

**Response**:
```json
[
  {
    "agent_name": "trader",
    "action": "BUY",
    "total": 25,
    "correct": 20,
    "accuracy": 80.0,
    "avg_return": 1.95,
    "avg_performance_score": 0.0145
  },
  {
    "agent_name": "trader",
    "action": "SELL",
    "total": 15,
    "correct": 10,
    "accuracy": 66.67,
    "avg_return": -1.20,
    "avg_performance_score": 0.0078
  },
  {
    "agent_name": "trader",
    "action": "HOLD",
    "total": 10,
    "correct": 6,
    "accuracy": 60.0,
    "avg_return": 0.15,
    "avg_performance_score": 0.0009
  }
]
```

**SQL 쿼리**:
```sql
SELECT
    agent_name,
    vote_action as action,
    COUNT(*) as total,
    SUM(CASE WHEN is_correct THEN 1 ELSE 0 END) as correct,
    ROUND(
        CAST(SUM(CASE WHEN is_correct THEN 1 ELSE 0 END) AS NUMERIC) /
        NULLIF(COUNT(*), 0) * 100, 2
    ) as accuracy,
    ROUND(AVG(return_pct), 4) as avg_return,
    ROUND(AVG(performance_score), 4) as avg_performance_score
FROM agent_vote_tracking
WHERE status = 'COMPLETED'
  AND agent_name = :agent  -- optional
GROUP BY agent_name, vote_action
ORDER BY agent_name, accuracy DESC
```

---

## 🎯 시스템 플로우

```
1. War Room Debate
   - 7 agents vote (trader, analyst, risk, macro, institutional, news, pm)
   ↓
2. Save to Database
   - ai_debate_sessions (전체 세션)
   - price_tracking (PM consensus)
   - agent_vote_tracking (개별 에이전트 투표) 🆕
   ↓
3. Wait 24 hours...
   ↓
4. Scheduler Runs
   - evaluate_pending_tracking() → price_tracking COMPLETED
   - evaluate_agent_votes_tracking() → agent_vote_tracking COMPLETED 🆕
   ↓
5. Performance Analysis
   - GET /api/performance/summary (consensus)
   - GET /api/performance/by-agent (에이전트별) 🆕
   - GET /api/performance/by-agent-action (에이전트+액션별) 🆕
   ↓
6. Self-Learning Feedback (Future - Phase 25.4)
   - 저성과 에이전트 경고
   - 가중치 자동 조정
   - 에이전트 프롬프트에 성과 피드백
```

---

## 📊 데이터 예시

### agent_vote_tracking 테이블

| id | session_id | agent_name | vote_action | vote_confidence | ticker | initial_price | final_price | return_pct | is_correct | performance_score | status |
|----|------------|------------|-------------|-----------------|--------|---------------|-------------|------------|------------|-------------------|--------|
| 1 | 11 | trader | BUY | 0.85 | AAPL | 178.50 | 180.20 | +0.95% | ✅ TRUE | +0.0081 | COMPLETED |
| 2 | 11 | analyst | BUY | 0.75 | AAPL | 178.50 | 180.20 | +0.95% | ✅ TRUE | +0.0071 | COMPLETED |
| 3 | 11 | risk | HOLD | 0.60 | AAPL | 178.50 | 180.20 | +0.95% | ❌ FALSE | -0.0057 | COMPLETED |
| 4 | 11 | macro | SELL | 0.55 | AAPL | 178.50 | 180.20 | +0.95% | ❌ FALSE | -0.0052 | COMPLETED |

---

## 🔧 주요 파일

### Backend

**Database**:
- [backend/database/create_agent_vote_tracking_table.sql](../../backend/database/create_agent_vote_tracking_table.sql) - 테이블 스키마
- [backend/scripts/create_agent_vote_tracking.py](../../backend/scripts/create_agent_vote_tracking.py) - 테이블 생성 스크립트

**API**:
- [backend/api/war_room_router.py:346-395](../../backend/api/war_room_router.py#L346-L395) - `save_agent_votes_tracking()`
- [backend/api/performance_router.py:363-420](../../backend/api/performance_router.py#L363-L420) - GET /api/performance/by-agent
- [backend/api/performance_router.py:423-493](../../backend/api/performance_router.py#L423-L493) - GET /api/performance/by-agent-action

**Automation**:
- [backend/automation/price_tracking_scheduler.py:254-405](../../backend/automation/price_tracking_scheduler.py#L254-L405) - `evaluate_agent_votes_tracking()`

---

## ✅ Phase 25.3 완료 체크리스트

### 데이터베이스
- [x] agent_vote_tracking 테이블 설계
- [x] 테이블 생성 스크립트
- [x] 인덱스 설정 (7개)
- [x] 제약 조건 (FOREIGN KEY, CHECK)

### 에이전트 투표 저장
- [x] save_agent_votes_tracking() 함수 구현
- [x] War Room 플로우에 통합
- [x] PM 에이전트 제외 로직
- [x] 에러 핸들링

### 평가 스케줄러
- [x] evaluate_agent_votes_tracking() 함수
- [x] PENDING 레코드 조회 (24h+ 경과)
- [x] 최종 가격 조회 (KIS API)
- [x] 수익률 계산
- [x] 성과 평가 (BUY/SELL/HOLD)
- [x] DB 업데이트 (COMPLETED/FAILED)
- [x] 로깅 및 요약 출력

### API 엔드포인트
- [x] GET /api/performance/by-agent
- [x] GET /api/performance/by-agent-action
- [x] 빈 데이터 처리
- [x] 에러 핸들링

---

## 🚀 실행 가이드

### 1. 테이블 생성
```bash
cd ai-trading-system
python backend/scripts/create_agent_vote_tracking.py
```

### 2. War Room 토론 (에이전트 투표 자동 저장)
```bash
# 프론트엔드에서 War Room 토론 시작
# 또는 API 호출
curl -X POST http://localhost:8001/api/war-room/debate \
  -H "Content-Type: application/json" \
  -d '{"ticker": "AAPL"}'
```

### 3. 24시간 후 평가 (수동)
```bash
python backend/automation/price_tracking_scheduler.py
# Consensus + Agent votes 모두 평가
```

### 4. 성과 조회 (API)
```bash
# 에이전트별 성과
curl http://localhost:8001/api/performance/by-agent

# 특정 에이전트의 액션별 성과
curl http://localhost:8001/api/performance/by-agent-action?agent=trader
```

---

## 📊 성과 분석 쿼리 예시

### 에이전트별 정확도
```sql
SELECT
    agent_name,
    COUNT(*) as total_votes,
    SUM(CASE WHEN is_correct THEN 1 ELSE 0 END) as correct,
    ROUND(AVG(CASE WHEN is_correct THEN 1 ELSE 0 END) * 100, 2) as accuracy_pct
FROM agent_vote_tracking
WHERE status = 'COMPLETED'
GROUP BY agent_name
ORDER BY accuracy_pct DESC;
```

### 저성과 에이전트 찾기 (정확도 50% 미만)
```sql
SELECT
    agent_name,
    COUNT(*) as total_votes,
    ROUND(AVG(CASE WHEN is_correct THEN 1 ELSE 0 END) * 100, 2) as accuracy_pct
FROM agent_vote_tracking
WHERE status = 'COMPLETED'
GROUP BY agent_name
HAVING AVG(CASE WHEN is_correct THEN 1 ELSE 0 END) < 0.5
ORDER BY accuracy_pct ASC;
```

### 에이전트+액션별 성과
```sql
SELECT
    agent_name,
    vote_action,
    COUNT(*) as total,
    AVG(return_pct) as avg_return,
    AVG(performance_score) as avg_score
FROM agent_vote_tracking
WHERE status = 'COMPLETED'
GROUP BY agent_name, vote_action
ORDER BY agent_name, avg_score DESC;
```

### 고신뢰도 but 저성과 에이전트
```sql
SELECT
    agent_name,
    AVG(vote_confidence) as avg_confidence,
    AVG(CASE WHEN is_correct THEN 1 ELSE 0 END) as accuracy
FROM agent_vote_tracking
WHERE vote_confidence > 0.8  -- 80% 이상 자신감
  AND status = 'COMPLETED'
GROUP BY agent_name
HAVING AVG(CASE WHEN is_correct THEN 1 ELSE 0 END) < 0.5  -- 하지만 50% 미만 정확도
ORDER BY accuracy ASC;
```

---

## 💡 다음 단계 (Phase 25.4 - 가중치 자동 조정 & 경고 시스템)

### 1. 성과 기반 가중치 자동 조정
```python
def calculate_agent_weights():
    """
    에이전트 성과 기반 가중치 계산

    Logic:
        - 정확도 70% 이상: weight = 1.2
        - 정확도 60-70%: weight = 1.0
        - 정확도 50-60%: weight = 0.8
        - 정확도 50% 미만: weight = 0.5
    """
    query = """
        SELECT
            agent_name,
            AVG(CASE WHEN is_correct THEN 1 ELSE 0 END) as accuracy
        FROM agent_vote_tracking
        WHERE status = 'COMPLETED'
        GROUP BY agent_name
    """

    weights = {}
    for agent, accuracy in results:
        if accuracy >= 0.7:
            weights[agent] = 1.2
        elif accuracy >= 0.6:
            weights[agent] = 1.0
        elif accuracy >= 0.5:
            weights[agent] = 0.8
        else:
            weights[agent] = 0.5

    return weights
```

### 2. 저성과 에이전트 경고 시스템
```python
def check_low_performing_agents():
    """
    저성과 에이전트 체크 및 경고
    """
    query = """
        SELECT agent_name, AVG(CASE WHEN is_correct THEN 1 ELSE 0 END) as accuracy
        FROM agent_vote_tracking
        WHERE status = 'COMPLETED'
        GROUP BY agent_name
        HAVING AVG(CASE WHEN is_correct THEN 1 ELSE 0 END) < 0.5
    """

    for agent, accuracy in low_performers:
        logger.warning(f"⚠️ Low performing agent: {agent} ({accuracy*100:.1f}%)")

        # 알림 전송
        send_alert(
            level="WARNING",
            title=f"Low Performing Agent: {agent}",
            message=f"Accuracy: {accuracy*100:.1f}% (below 50% threshold)"
        )
```

### 3. 신뢰도 vs 실제 성과 분석
```python
def analyze_confidence_accuracy_gap():
    """
    에이전트의 자신감과 실제 성과 차이 분석
    """
    query = """
        SELECT
            agent_name,
            AVG(vote_confidence) as avg_confidence,
            AVG(CASE WHEN is_correct THEN 1 ELSE 0 END) as accuracy,
            ABS(AVG(vote_confidence) - AVG(CASE WHEN is_correct THEN 1 ELSE 0 END)) as gap
        FROM agent_vote_tracking
        WHERE status = 'COMPLETED'
        GROUP BY agent_name
        ORDER BY gap DESC
    """

    # 자신감은 높지만 정확도 낮은 에이전트 → 오버컨피던트
    # 자신감은 낮지만 정확도 높은 에이전트 → 언더컨피던트
```

### 4. 학습 데이터 피드백
```python
# 에이전트 시스템 프롬프트에 성과 추가
system_prompt = f"""
You are the {agent_name} agent in a multi-agent trading system.

📊 Your Recent Performance (Last 50 votes):
- Accuracy: {agent_accuracy}%
- Average Return: {agent_avg_return}%
- Best Action: {agent_best_action} ({best_action_accuracy}%)
- Worst Action: {agent_worst_action} ({worst_action_accuracy}%)
- Confidence vs Accuracy Gap: {confidence_gap}%

💡 Performance Insights:
{performance_insights}

Based on your historical performance, please adjust your analysis strategy:
- Be more cautious when voting {worst_action}
- Leverage your strength in {best_action} analysis
- Calibrate your confidence levels (current gap: {confidence_gap}%)
"""
```

---

## 📝 알려진 이슈

### Issue #1: 가중치 수동 설정
**현재**: 모든 에이전트 동일 가중치 (1.0)
**필요**: 성과 기반 자동 가중치 조정
**우선순위**: High

### Issue #2: 경고 시스템 부재
**현재**: 저성과 에이전트 수동 확인
**필요**: 자동 경고 알림 시스템
**우선순위**: Medium

### Issue #3: 피드백 루프 없음
**현재**: 에이전트가 자신의 성과를 모름
**필요**: 성과 데이터를 프롬프트에 피드백
**우선순위**: High

---

## 🔗 관련 Phase

**Phase 25.1**: [24시간 수익률 추적 시스템](./251223_Phase25.1_24시간_수익률_추적_완료.md)
**Phase 25.2**: [성과 대시보드 UI](./251223_Phase25.2_성과대시보드_완료.md)

---

**작성**: 2025-12-23
**상태**: ✅ Phase 25.3 완료
**다음**: Phase 25.4 - 가중치 자동 조정 & 경고 시스템
