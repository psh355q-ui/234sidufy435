# 🎯 Dividend Optimizer System
## 분석 로직 및 설치 가이드

---

# 📑 목차

1. **시스템 개요**
2. **분석 로직 상세**
3. **포트폴리오 최적화 알고리즘**
4. **배당 지속성 분석**
5. **리스크 분석**
6. **백테스트 엔진**
7. **설치 및 실행 가이드**

---

# 1️⃣ 시스템 개요

## 목적
> 월 목표 현금흐름을 달성하기 위한 **AI 기반 배당 포트폴리오 최적화 시스템**

## 핵심 기능
| 기능 | 설명 |
|------|------|
| 🎯 **목표 기반 설계** | 월 100만원 등 목표 설정 → 필요 자본/포트폴리오 자동 계산 |
| 📊 **10개 투자 테마** | 월배당, 실버연금, 성장배당, 커버드콜 등 |
| ⚖️ **3단계 리스크 티어** | 방어형 / 균형형 / 공격형 |
| 🔬 **고급 최적화** | Risk Parity, Max Sharpe, Mean-Variance |
| 📈 **백테스트** | 과거 수익률 시뮬레이션 |

## 기술 스택
```
Backend:  Python 3.9+ / Flask / yfinance / scipy
Frontend: HTML5 / Tailwind CSS / Chart.js
Data:     JSON 기반 설정 / yfinance 실시간 데이터
```

---

# 2️⃣ 분석 로직 상세

## 2.1 데이터 수집 (loader.py)

### 수집 데이터
```python
# yfinance에서 가져오는 정보
{
    "price": 현재가,
    "yield": TTM 배당 수익률 (12개월 누적),
    "frequency": 지급 빈도 (월배당/분기/연간),
    "payments": [{date, amount}, ...],  # 개별 배당 지급 내역
    "sector": 섹터
}
```

### TTM 배당률 계산
```python
# Trailing Twelve Month (TTM) 배당률
ttm_dividend = 최근_12개월_배당금_합계
dividend_yield = ttm_dividend / current_price

# 예: JEPI
# 12개월 배당: $5.40 / 주가: $55 = 9.8% yield
```

### 지급 빈도 판별
```python
payments_per_year = len(recent_12_months_payments)

if payments >= 10:    frequency = "Monthly"
elif payments >= 3:   frequency = "Quarterly"
else:                 frequency = "Annual"
```

---

## 2.2 유니버스 필터링 (engine.py)

### 태그 기반 필터링
```
📦 Universe Seed (214개 티커)
    ↓
🏷️ 태그 필터링 (allowed_tags / banned_tags)
    ↓
📊 배당 데이터 존재 여부 확인
    ↓
✅ 적격 유니버스 (Eligible Universe)
```

### 태그 시스템 예시
```json
{
    "SCHD": ["dividend_quality", "core", "quarterly"],
    "JEPI": ["covered_call", "high_yield", "monthly_payer"],
    "O":    ["reit", "monthly_payer", "dividend_quality"]
}
```

### 테마별 필터 규칙 예시
```python
# 테마: "월배당 최고로" (max_monthly_income)
# 티어: 방어형 (defensive)

allowed_tags = ["covered_call", "dividend_quality", "monthly_payer", "core"]
banned_tags = ["mreits"]  # 모기지 리츠 제외 (변동성 높음)

# 제약 조건
constraints = {
    "etf_min": 0.6,           # ETF 최소 60%
    "single_stock_max": 0.08,  # 개별주 최대 8%
    "max_tag_weight": {
        "high_yield": 0.45,    # 고배당 태그 최대 45%
        "covered_call": 0.40
    }
}
```

---

## 2.3 목표 자본 역산

### 계산 공식
```
목표 월 현금흐름 (KRW) ← 사용자 입력 (예: 100만원)
    ↓
USD 변환: 1,000,000 / 1,420 = $704/월
    ↓
세전 연간 필요: ($704 × 12) / (1 - 0.154) = $10,000
    ↓
포트폴리오 배당률: 6.5% (가중 평균)
    ↓
필요 자본: $10,000 / 0.065 = $153,846
    ↓
KRW 환산: $153,846 × 1,420 = 약 2.2억원
```

### 코드 구현
```python
def calculate_required_capital(
    target_monthly_krw: float,
    fx_rate: float,
    tax_rate: float,
    portfolio_yield: float
) -> float:
    target_monthly_usd = target_monthly_krw / fx_rate
    target_annual_pretax = (target_monthly_usd * 12) / (1 - tax_rate)
    required_capital_usd = target_annual_pretax / portfolio_yield
    return required_capital_usd * fx_rate
```

---

# 3️⃣ 포트폴리오 최적화 알고리즘

## 3.1 Greedy 방식 (기본)

### 알고리즘
```
1. ETF를 배당률 내림차순 정렬
2. ETF 최소 비중(예: 60%)을 채움
3. 개별주를 배당률 내림차순 정렬
4. 남은 비중을 개별주로 채움 (최대 비중 제한)
5. 전체 비중을 100%로 정규화
```

### 장점/단점
| 장점 | 단점 |
|------|------|
| 빠른 계산 | 배당률만 고려 |
| 직관적 이해 | 리스크 분산 미흡 |

---

## 3.2 Risk Parity (동일 위험 기여)

### 개념
> 각 자산이 포트폴리오 전체 리스크에 **동일한 기여**를 하도록 비중 조절

### 수학적 정의
```
Risk Contribution (RC_i) = w_i × (Σ @ w / σ_p)

목표: RC_1 = RC_2 = ... = RC_n
```

### 코드 구현
```python
def risk_parity_objective(weights, cov_matrix):
    """목적 함수: 리스크 기여도 편차 최소화"""
    portfolio_vol = np.sqrt(weights.T @ cov_matrix @ weights)
    
    # 한계 위험 기여도 (MRC)
    marginal_risk = cov_matrix @ weights / portfolio_vol
    
    # 위험 기여도 (RC)
    risk_contribution = weights * marginal_risk
    
    # 목표: 모든 RC가 동일
    target_rc = np.sum(risk_contribution) / len(weights)
    
    return np.sum((risk_contribution - target_rc) ** 2)
```

### 최적화 실행
```python
from scipy.optimize import minimize

result = minimize(
    risk_parity_objective,
    x0=initial_weights,      # 초기값: 동일 비중
    method='SLSQP',
    bounds=[(0.02, 0.25)] * n,  # 최소 2%, 최대 25%
    constraints={'type': 'eq', 'fun': lambda w: sum(w) - 1}
)
```

---

## 3.3 Max Sharpe Ratio (최대 샤프)

### 개념
> 위험 대비 수익을 **최대화**하는 비중 조합

### 수학적 정의
```
Sharpe Ratio = (E[R_p] - R_f) / σ_p

E[R_p]: 포트폴리오 기대 수익률
R_f:    무위험 이자율 (예: 5%)
σ_p:    포트폴리오 변동성
```

### 코드 구현
```python
def neg_sharpe_ratio(weights, returns, cov_matrix, rf=0.05):
    """목적 함수: Sharpe Ratio 최대화 (음수로 최소화)"""
    portfolio_return = weights.T @ returns * 252  # 연환산
    portfolio_vol = np.sqrt(weights.T @ cov_matrix @ weights) * np.sqrt(252)
    
    sharpe = (portfolio_return - rf) / portfolio_vol
    return -sharpe  # 최소화 문제로 변환
```

---

## 3.4 최적화 방식 비교

| 방식 | 목표 | 특징 | 적합한 경우 |
|------|------|------|------------|
| **Greedy** | 배당률 최대화 | 빠름, 단순 | 빠른 결과 필요 시 |
| **Risk Parity** | 위험 균등 분배 | 분산 우수 | 안정적 현금흐름 |
| **Max Sharpe** | 수익/위험 최적화 | 수익 극대화 | 성과 중시 |

---

# 4️⃣ 배당 지속성 분석

## 4.1 분석 지표

### Payout Ratio (배당 성향)
```python
payout_ratio = annual_dividend / earnings_per_share

# 해석
< 30%:  매우 안전 (성장 여력 큼)
30-50%: 안전
50-70%: 주의
> 70%:  위험 (감액 가능성)
```

### Dividend Growth Rate (배당 성장률)
```python
# 5년 CAGR
cagr = (last_year_dividend / first_year_dividend) ** (1/5) - 1

# 예: 5년 전 $2.00 → 현재 $3.00
# CAGR = (3.00/2.00)^0.2 - 1 = 8.4%
```

### Dividend Streak (연속 지급 연수)
```python
# 배당을 끊기지 않고 지급한 연수
streak = count_consecutive_dividend_years()

# Dividend Aristocrats: 25년 이상 연속 증가
# Dividend Kings: 50년 이상 연속 증가
```

---

## 4.2 Safety Score (배당 안정성 점수)

### 점수 체계 (100점 만점)
```
Payout Ratio (30점)
├── < 30%:  30점
├── < 50%:  25점
├── < 70%:  15점
└── > 70%:  5점

Dividend Growth (25점)
├── > 10%:  25점
├── > 5%:   20점
├── > 0%:   15점
└── < 0%:   5점

Streak (25점)
├── ≥ 25년: 25점
├── ≥ 10년: 20점
├── ≥ 5년:  15점
└── < 5년:  streak × 2점

총점 → 등급
├── 80+:  A (매우 안전)
├── 60+:  B (안전)
├── 40+:  C (주의)
└── < 40: D (위험)
```

---

# 5️⃣ 리스크 분석

## 5.1 변동성 (Volatility)
```python
# 일간 수익률의 표준편차 → 연환산
daily_returns = np.log(prices / prices.shift(1))
annual_volatility = daily_returns.std() * np.sqrt(252)

# 해석
< 15%:  저변동성 (유틸리티, 필수소비재)
15-25%: 중간
> 25%:  고변동성 (성장주, 소형주)
```

## 5.2 최대 낙폭 (Max Drawdown)
```python
rolling_max = prices.expanding().max()
drawdown = (prices - rolling_max) / rolling_max
max_drawdown = drawdown.min()

# 해석
> -10%:  안정적
-10~-20%: 보통
< -30%:  고위험
```

## 5.3 샤프 비율 (Sharpe Ratio)
```python
sharpe = (annual_return - risk_free_rate) / annual_volatility

# 해석
> 1.0:   우수
0.5-1.0: 양호
< 0.5:   저조
```

---

# 6️⃣ 백테스트 엔진

## 6.1 시뮬레이션 프로세스

```
1. 과거 가격 데이터 수집 (yfinance)
2. 포트폴리오 비중대로 자산 배분
3. 일간 수익률 계산 → 누적 수익률
4. 배당금 수집 → 총 배당 수익 계산
5. 벤치마크(SPY) 대비 성과 비교
```

## 6.2 출력 지표

| 지표 | 설명 |
|------|------|
| **Total Return** | 총 수익률 (가격 + 배당) |
| **Price Return** | 가격 상승분만 |
| **Dividend Return** | 배당 수익분만 |
| **CAGR** | 연평균 복합 성장률 |
| **Max Drawdown** | 최대 낙폭 |
| **Sharpe Ratio** | 위험조정 수익률 |
| **Alpha** | 벤치마크 대비 초과 수익 |

## 6.3 코드 예시
```python
result = backtest_engine.run_backtest(
    portfolio=[
        ("SCHD", 0.30),
        ("JEPI", 0.25),
        ("O", 0.15),
        ("VIG", 0.15),
        ("VYM", 0.15)
    ],
    start_date="2022-01-01",
    initial_capital=100000
)

# 결과
{
    "total_return": 0.156,      # 15.6%
    "cagr": 0.078,              # 7.8%
    "max_drawdown": -0.145,     # -14.5%
    "sharpe_ratio": 0.92,
    "alpha": 0.023              # SPY 대비 +2.3%
}
```

---

# 7️⃣ 설치 및 실행 가이드

## 7.1 사전 요구사항

```bash
# Python 3.9 이상
python --version
# Python 3.9.x 또는 그 이상

# pip 최신화
pip install --upgrade pip
```

## 7.2 프로젝트 구조 생성

```bash
# 1. 프로젝트 폴더 생성
mkdir dividend-optimizer
cd dividend-optimizer

# 2. 하위 폴더 생성
mkdir -p us_market/dividend/{config,data}
mkdir -p templates
```

## 7.3 의존성 설치

```bash
# requirements.txt 생성
cat > requirements.txt << EOF
flask>=2.0
yfinance>=0.2.0
pandas>=1.5
numpy>=1.20
scipy>=1.9
EOF

# 설치
pip install -r requirements.txt
```

## 7.4 설정 파일 생성

### tags.json
```bash
# us_market/dividend/config/tags.json
# 28개 태그 정의 (Blueprint BACKEND_STEP1 참조)
```

### dividend_plans.json
```bash
# us_market/dividend/config/dividend_plans.json
# 10개 테마 × 3개 티어 = 30개 조합 정의
# (Blueprint CONFIG_STEP1, CONFIG_STEP2 참조)
```

### universe_seed.json
```bash
# us_market/dividend/data/universe_seed.json
# 214개 티커 (ETF 88 + 개별주 126)
# (Blueprint DATA_STEP1, DATA_STEP2 참조)
```

## 7.5 Python 모듈 생성

```bash
# Blueprint 참고하여 생성
# BACKEND_STEP2.md → loader.py
# BACKEND_STEP3.md → engine.py, portfolio_optimizer.py, 
#                    risk_analytics.py, dividend_analyzer.py, backtest.py
# BACKEND_STEP4.md → flask_app.py 내 라우트
```

## 7.6 배당 데이터 수집

```bash
# 최초 실행 시 배당 데이터 수집 (약 5-10분 소요)
python -c "
from us_market.dividend.loader import DividendDataLoader
loader = DividendDataLoader()
loader.fetch_data()
"

# 결과: us_market/dividend/data/dividend_universe.json 생성
```

## 7.7 서버 실행

```bash
# Flask 서버 시작
python flask_app.py

# 출력
# 🚀 Flask Server Starting on port 5001...
# * Running on http://127.0.0.1:5001
```

## 7.8 접속

```
브라우저에서:
http://localhost:5001          → 랜딩 페이지
http://localhost:5001/dividend → 배당 최적화 UI
```

---

# 📊 시스템 아키텍처 요약

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (HTML/JS)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐   │
│  │ index    │  │dashboard │  │ dividend.html        │   │
│  │ .html    │  │ .html    │  │ (테마선택/결과표시)   │   │
│  └──────────┘  └──────────┘  └──────────────────────┘   │
└─────────────────────────┬───────────────────────────────┘
                          │ API 호출
┌─────────────────────────▼───────────────────────────────┐
│                 Flask API (flask_app.py)                 │
│  /api/dividend/themes      → 테마 목록                   │
│  /api/dividend/all-tiers   → 3개 티어 포트폴리오         │
│  /api/dividend/risk-metrics → 리스크 분석               │
│  /api/dividend/backtest    → 백테스트                    │
└─────────────────────────┬───────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────┐
│                   Backend Modules                        │
│  ┌──────────┐  ┌───────────┐  ┌────────────────────┐   │
│  │ engine   │  │ optimizer │  │ dividend_analyzer  │   │
│  │ .py      │  │ .py       │  │ .py                │   │
│  └──────────┘  └───────────┘  └────────────────────┘   │
│  ┌──────────┐  ┌───────────┐                            │
│  │ loader   │  │ backtest  │                            │
│  │ .py      │  │ .py       │                            │
│  └──────────┘  └───────────┘                            │
└─────────────────────────┬───────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────┐
│                    Data Sources                          │
│  ┌───────────────┐  ┌─────────────────────────────────┐ │
│  │ JSON Config   │  │ yfinance (실시간 시세/배당)      │ │
│  │ (테마/티커)   │  │                                  │ │
│  └───────────────┘  └─────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

# 🎯 핵심 요약

| 항목 | 내용 |
|------|------|
| **목표** | 월 목표 현금흐름 달성을 위한 배당 포트폴리오 자동 설계 |
| **테마** | 10개 (월배당, 실버연금, 성장배당, 커버드콜 등) |
| **티어** | 3개 (방어형/균형형/공격형) |
| **유니버스** | 214개 티커 (ETF 88 + 개별주 126) |
| **최적화** | Greedy, Risk Parity, Max Sharpe |
| **분석** | 배당 안정성, 리스크 지표, 백테스트 |
| **기술** | Python + Flask + yfinance + scipy |

---

*Last Updated: 2025-12-20*
